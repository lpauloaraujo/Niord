[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "pwdlib[argon2]>=0.3.0",
    "redis[hiredis]>=7.4.0",
    "sqlalchemy>=2.0.48",
]
